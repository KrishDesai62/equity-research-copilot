"""
Phase 5 (post-MVP). A fixed rule using the same signals the portfolio
agent sees (e.g. "buy when distress_prob drops and sentiment score
rises"), with NO LLM judgment involved. This exists specifically so you
can measure whether the agent adds value over a dumb rule using
identical inputs — without this file, "the agent helped" is an assumed
claim, not a measured one.
"""
# TODO: build in Phase 5.
